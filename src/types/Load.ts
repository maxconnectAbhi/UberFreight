export type Load = {
  id: string;
  price: string;
  origin: string;
  originDate: string;
  destination: string;
  destinationDate: string;
  status: string;
  distance?: string;
  deadhead?: string;
};